# ===========================================
# Personal Finance Tracker - Day 1 Environment Variables
# ===========================================

# Database Configuration (PostgreSQL)
# Update these with your actual PostgreSQL credentials
DB_HOST=localhost
DB_PORT=5432
DB_NAME=finance_tracker
DB_USER=your_postgres_username
DB_PASSWORD=your_postgres_password

# JWT Configuration
# IMPORTANT: Generate strong secrets for production!
JWT_SECRET=your_very_secure_jwt_secret_minimum_32_characters
JWT_EXPIRES_IN=1h

# Server Configuration
PORT=3000
NODE_ENV=development

# Rate Limiting (Optional - has defaults)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# ===========================================
# Setup Instructions:
# 1. Copy this file to .env
# 2. Update DB_USER and DB_PASSWORD with your PostgreSQL credentials
# 3. Generate a strong JWT_SECRET (32+ characters)
# 4. Create the database: createdb finance_tracker
# 5. Run migrations: npm run migrate
# 6. Start server: npm run dev
# ==========================================